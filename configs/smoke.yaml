{
  "model_name_or_path": "jhu-clsp/ettin-encoder-400m",
  "model_revision": "7662476d60abb071a5bd319c9f3074f3072c062d",
  "max_length": 1024,
  "max_candidates": 16,
  "seed": 42,
  "per_device_train_batch_size": 1,
  "per_device_eval_batch_size": 1,
  "gradient_accumulation_steps": 32,
  "num_train_epochs": 1,
  "max_steps": 3,
  "learning_rate": 0.00002,
  "weight_decay": 0.01,
  "warmup_ratio": 0.03,
  "bf16": true,
  "gradient_checkpointing": true,
  "eval_strategy": "steps",
  "save_strategy": "steps",
  "eval_steps": 1,
  "save_steps": 1,
  "logging_steps": 1,
  "save_total_limit": 2,
  "report_to": "none",
  "max_train_per_source": 32,
  "max_train_per_task": 16,
  "max_eval_per_source": 16,
  "scan_limit_per_split": 2000,
  "bigbench_configs": ["analytic_entailment", "logical_deduction", "sports_understanding"]
}
