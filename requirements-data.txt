# Install only on the remote CPU preparation machine, in a Python 3.12 venv.
# Plain Transformers/Datasets keep Torch optional; do not add [torch] extras.
transformers==5.16.1
datasets==4.8.5
huggingface-hub>=1.5.0,<2.0
